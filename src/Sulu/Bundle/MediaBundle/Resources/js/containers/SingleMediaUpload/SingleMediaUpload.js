// @flow
import React from 'react';

type Props = {};

export default class SingleMediaUpload extends React.PureComponent<Props> {
    render() {
        return (
            <div />
        );
    }
}
