This component shows selects for the available segments. If the component is used for a specific webspace, then only a
single Select is shown, otherwise it shows a separate select for every webspace.
