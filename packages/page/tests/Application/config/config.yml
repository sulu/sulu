framework:
    # symfony recipe default configuration:
    workflows: null
    messenger:
        transports:
        routing:
    profiler: { enabled: true, collect: false }

twig:
    paths:
        - '%kernel.project_dir%/templates'
