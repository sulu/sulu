// @flow
import React from 'react';

type Props = {||};

export default class TeaserSelection extends React.Component<Props> {
    render() {
        return (
            <div>Teaser!</div>
        );
    }
}
