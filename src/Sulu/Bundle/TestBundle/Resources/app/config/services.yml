services:
    sulu_test.doctrine.orm.default_entity_manager:
        alias: doctrine.orm.default_entity_manager
        public: true

    sulu_test.doctrine_phpcr.default_session:
        alias: doctrine_phpcr.default_session
        public: true

    sulu_test.doctrine_phpcr:
        alias: doctrine_phpcr
        public: true

    sulu_test.doctrine_phpcr.live_session:
        alias: doctrine_phpcr.live_session
        public: true

    sulu_test.doctrine_phpcr.session:
        alias: doctrine_phpcr.session
        public: true

    sulu_test.massive_search.adapter:
        alias: massive_search.adapter
        public: true

    sulu_test.massive_search.adapter.test:
        alias: massive_search.adapter.test
        public: true
