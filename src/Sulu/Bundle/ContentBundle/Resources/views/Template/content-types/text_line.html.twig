<input type="text"
       class="form-element preview-update"
       id="{{ id }}"
       data-mapper-property="{{ property.name }}"
        {% if property.mandatory %}
    data-validation-required="true"
        {% endif %}/>
