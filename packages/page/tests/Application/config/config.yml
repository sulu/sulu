sulu_core:
    content:
        structure:
            default_type:
                page: 'page'

framework:
    # symfony recipe default configuration:
    workflows: null
    messenger:
        transports:
        routing:

twig:
    paths:
        - '%kernel.project_dir%/templates'
