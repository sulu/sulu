sulu_route: {}

framework:
    # symfony recipe default configuration:
    workflows: null
    messenger:
        transports:
        routing:

twig:
    paths:
        - '%kernel.project_dir%/templates'
