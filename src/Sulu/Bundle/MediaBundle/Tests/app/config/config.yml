massive_search:
    adapter_id: massive_search.adapter.test

sulu_media:
    search:
        enabled: true
    format_manager:
        response_headers:
            Expires: "+1 month"
    format_cache:
        service:
            type: local
    storage:
        service:
            type: local
