This component shows how many characters are left to type.

```javascript
<CharacterCounter max={10} value="Hello" />
```

It also shows in red if too many characters have been used.

```javascript
<CharacterCounter max={10} value="Hello World!" />
```
