<input type="text"
       class="form-element"
       id="{{ property.id }}"
       data-mapper-property="{{ property.id }}"
        {% if property.mandatory %}
    data-validation-required="true"
        {% endif %}/>
