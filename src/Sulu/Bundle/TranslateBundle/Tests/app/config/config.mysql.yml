imports:
    - { resource: config.yml }

doctrine:
    dbal:
        driver:   pdo_mysql
        host:     localhost
        dbname:   sulu_test
        user:     root
        password:
        charset:  UTF8
    orm:
        auto_generate_proxy_classes: %kernel.debug%
        auto_mapping: true
