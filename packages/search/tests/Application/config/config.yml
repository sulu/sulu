sulu_article:
    default_main_webspace: 'sulu-io'

framework:
    # symfony recipe default configuration:
    workflows: null
    messenger:
        transports:
            sync: 'sync://'
        routing:

twig:
    paths:
        - '%kernel.project_dir%/templates'
